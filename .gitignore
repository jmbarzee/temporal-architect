# Dependencies
node_modules/

# VS Code Extension build artifacts
packages/vscode/out/
packages/vscode/dist/
packages/vscode/bin/
packages/vscode/skills/
*.vsix

# Claude Code plugin payload — built by `make build-claude-plugin`,
# copied from the canonical skills/ at the repo root, never tracked.
packages/npm/claude-plugin/skills/

# Publish tokens (contains secrets)
.env.publish

# Visualizer build artifacts
tools/visualizer/dist/
tools/visualizer/dist-lib/

# LICENSE copy generated by the visualizer package's prepack hook
# (the canonical copy lives at the repo root)
tools/visualizer/LICENSE

# Release packaging staging
dist/

# Built Go binaries
tools/lsp/twf

twf

# Claude settings
.claude/settings.local.json
examples/.claude/settings.local.json
internal/orchestrator/.claude/settings.local.json

# skills used to build examples
examples/.claude/skills

./bump-brew